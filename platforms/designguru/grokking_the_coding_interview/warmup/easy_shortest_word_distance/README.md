# Shortest Word Distance

- Pattern: Two pointers

## Problem

Given a string array `words` and two string `word1`, `word2`, return the shortest distances between `word1` and `word2`.

## Notes

- Time: O(n)
- Space: O(1)
- Approach: use two pointers and iteratively update the shortest distance
