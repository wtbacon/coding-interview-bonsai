# Sqrt

- Pattern: Binary Search

## Problem

Given a non-negative integer `x`, return the square root of `x` rounded down to the nearest integer.

## Notes

- Time: $O(log{n})$
- Space: $O(1)$
- Approach: Use the binary search and when the loop finishes, `high` is the element I want to get.
